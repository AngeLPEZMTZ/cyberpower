    <!-- Encabezado -->
    <header class="header">
        <div class="container header-container">
            <div class="logo">
            <a href="/inicio" class="logo-link">
            CYBERPOWER
            </a>
            </div>
            <div class="header-icons">
                <div class="search-box">
    <form action="/buscar" method="GET" style="display: flex; align-items: center;">
        <input type="text" name="query" placeholder="Buscar hardware..." class="search-input">
    </form>
</div>

<div class="user-actions">
    <% if (locals.usuario) { %>
    <a href="/editarperfil" class="user-name-link">
    <%= usuario %>
        </a>
    <% } else { %>
        <a href="/login-page" target="_blank" class="profile-link">
            <span class="icon-user"></span>
        </a>
    <% } %>
</div>

<a href="/carrito" class="icon-cart" style="position: relative; text-decoration: none;">    
    <% if (typeof carrito !== 'undefined' && carrito.length > 0) { %>
        <span class="cart-count-badge">
            <%= carrito.length %>
        </span>
    <% } %>
</a>


    </header>